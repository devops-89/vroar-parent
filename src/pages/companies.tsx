import CompanyLayout from "@/components/Companies";
import { Box } from "@mui/material";
import React from "react";

const Companies = () => {
  return (
    <Box>
      <CompanyLayout />
    </Box>
  );
};

export default Companies;
